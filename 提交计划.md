# Git 提交计划

基于 `.github/COMMIT_CONVENTION.md` 规范，将包集成工作整理为 6 个原子提交。

---

## 当前状态

- ✅ 6/7 包已集成到项目（85.7%）
- ✅ 旧代码已清理
- ✅ 新适配器和包装组件已创建
- ✅ pubspec.yaml 已配置本地路径依赖
- ✅ pubspec_local.yaml 已创建（GitHub 依赖备用）
- ✅ ZenThemeKit 和 ZenUIKit 职责已分离
  - ZenThemeKit：主题管理系统（ZenTheme, ZenThemeData, ZenColors）
  - ZenUIKit：纯 UI 组件库（Button, Container, Display + 工具类）
- ✅ ZenUIKit 组件已重构为纯 UI 组件（接受颜色参数）
- ✅ 主项目组件已更新以传递必需参数
- ✅ 项目可以正常编译和运行

---

## 提交方案（遵循规范）

### 重要说明

在执行提交前，已完成以下修复工作（这些修改在 packages/ 目录中，不会被提交）：

1. **ZenUIKit 组件重构**
   - `zen_button.dart`: 改为接受 `backgroundColor`、`shadowLight`、`shadowDark` 参数
   - `zen_container.dart`: 改为接受 `backgroundColor`、`shadowLight`、`shadowDark` 参数
   - `zen_display.dart`: 改为接受可选颜色参数，提供默认值
   - 移除所有组件对 `zen_theme.dart` 的导入和依赖

2. **ZenUIKit 测试清理**
   - 删除 `packages/ZenUIKit/test/zen_theme_test.dart`（主题测试应在 ZenThemeKit 中）
   - 更新 `packages/ZenUIKit/test/zen_display_test.dart`（移除 ZenTheme 依赖）

3. **归档文件清理**
   - 删除 `docs/archive/zenquotekit_v0.1.0_complete.dart`（被 Flutter 分析器误识别）

4. **主项目组件更新**
   - `lib/app/components/zen_calc_button.dart`: 从 ZenTheme 获取颜色并传递给 ZenButton

这些修复确保了：
- ✅ ZenUIKit 是真正的纯 UI 组件库（零依赖）
- ✅ ZenThemeKit 和 ZenUIKit 完全独立
- ✅ 主项目可以正常编译和运行
- ✅ 主项目 lib/ 目录 0 个编译错误

---

### Commit 1: chore(deps): 配置包依赖和忽略规则

**类型：** chore（构建/依赖变更）  
**范围：** deps（依赖管理）  
**标题：** chore(deps): 配置包依赖和忽略规则

**包含文件：**

```text
修改：
  .gitignore                    # 忽略 packages/ 和 docs/archive/
  pubspec.yaml                  # 本地路径依赖
  pubspec.lock                  # 依赖锁定文件
  
新增：
  pubspec_local.yaml            # GitHub 依赖备用配置
```

**提交信息：**

```text
chore(deps): 配置包依赖和忽略规则

- 配置 7 个包的本地路径依赖
- 添加 pubspec_local.yaml 作为 GitHub 依赖备用
- 更新 .gitignore 忽略 packages/ 和 docs/archive/

包列表：
- ArithmeticKit (计算逻辑)
- ConversionKit (单位换算)
- ZenUIKit (UI 组件)
- ZenThemeKit (主题管理)
- ZenQuoteKit (禅语服务)
- FeedbackKit (反馈系统)
- HistoryKit (历史记录)
```

---

### Commit 2: refactor(feedback): 集成 FeedbackKit

**类型：** refactor（重构）  
**范围：** feedback（反馈系统）  
**标题：** refactor(feedback): 集成 FeedbackKit

**包含文件：**

```text
删除：
  lib/app/services/haptic_service.dart
  lib/app/services/audio_service.dart

新增：
  lib/app/components/zen_calc_button.dart

修改：
  lib/app/modules/calculator/views/calculator_view.dart
  lib/app/modules/calculator/views/widgets/calculator_top_bar.dart
  lib/app/modules/history/views/history_view.dart
```

**提交信息：**

```text
refactor(feedback): 集成 FeedbackKit

- 删除本地反馈服务实现
- 使用 FeedbackKit 的 HapticService 和 AudioService
- 创建 zen_calc_button.dart 包装组件
- 更新 4 个文件的导入和使用

功能保持：
- 触觉反馈（轻、中、重、选择）
- 音频反馈（数字、运算符、等于、清除）
```

---

### Commit 3: refactor(history): 集成 HistoryKit

**类型：** refactor（重构）  
**范围：** history（历史记录）  
**标题：** refactor(history): 集成 HistoryKit

**包含文件：**

```text
删除：
  lib/app/services/history_service.dart

新增：
  lib/app/services/calculation_history_service.dart

修改：
  lib/app/modules/calculator/views/calculator_view.dart
  lib/app/modules/history/views/history_view.dart
  lib/app/modules/history/controllers/history_controller.dart
```

**提交信息：**

```text
refactor(history): 集成 HistoryKit

- 删除本地历史服务实现
- 创建 calculation_history_service.dart 适配器
- 使用 HistoryKit 的通用历史管理
- 更新 3 个文件使用新服务

功能增强：
- 通用历史记录模型
- 搜索和过滤功能
- 统计分析功能
```

---

### Commit 4: refactor(calculator): 集成 ArithmeticKit 和 ZenQuoteKit

**类型：** refactor（重构）  
**范围：** calculator（计算器）  
**标题：** refactor(calculator): 集成 ArithmeticKit 和 ZenQuoteKit

**包含文件：**

```text
删除：
  lib/app/utils/calculator_logic.dart
  lib/app/utils/scientific_calculator_logic.dart
  lib/app/services/zen_quote_service.dart
  lib/app/components/zen_quote_widget.dart

新增：
  lib/app/services/zen_settings_service.dart

修改：
  lib/app/modules/calculator/views/calculator_view.dart
```

**提交信息：**

```text
refactor(calculator): 集成 ArithmeticKit 和 ZenQuoteKit

- 删除本地计算逻辑实现
- 删除本地禅语服务实现
- 使用 ArithmeticKit 的 BasicCalculator
- 使用 ZenQuoteKit 的禅语服务和组件
- 添加 zen_settings_service.dart 管理设置

功能增强：
- 表达式计算支持
- 多语言禅语（中文、英文、日文）
- 上下文感知的禅语推荐
```

---

### Commit 5: refactor(ui): 集成 ZenUIKit 和 ZenThemeKit

**类型：** refactor（重构）  
**范围：** ui（UI 组件）  
**标题：** refactor(ui): 集成 ZenUIKit 和 ZenThemeKit

**包含文件：**

```text
删除：
  lib/app/components/neumorphic_button.dart
  lib/app/components/neumorphic_container.dart
  lib/app/components/neumorphic_display.dart

新增：
  lib/app/components/zen_calc_display.dart
  lib/app/config/theme/zen_calc_colors.dart

修改：
  lib/main.dart
  lib/app/config/theme/app_theme.dart
  lib/app/modules/calculator/views/widgets/basic_button_grid.dart
  lib/app/modules/calculator/views/widgets/scientific_button_grid.dart
  lib/app/components/zen_calc_button.dart (已在 Commit 2 中新增)
```

**提交信息：**

```text
refactor(ui): 集成 ZenUIKit 和 ZenThemeKit

ZenUIKit（纯 UI 组件库）：
- 重构为接受颜色参数的纯组件
- ZenButton 接受 backgroundColor、shadowLight、shadowDark
- ZenContainer 接受 backgroundColor、shadowLight、shadowDark
- ZenDisplay 接受可选颜色参数，提供默认值
- 删除对 ZenTheme 的依赖
- 删除 zen_theme_test.dart（主题测试移到 ZenThemeKit）

ZenThemeKit（主题管理）：
- 使用 ZenTheme 主题系统
- 使用 ZenThemeData 管理主题数据
- 提供 SandGarden 和 BambooForest 预设

主项目集成：
- 删除旧的 Neumorphic 组件
- zen_calc_button 从 ZenTheme 获取颜色并传递给 ZenButton
- zen_calc_display 使用 ZenTheme 获取颜色
- 使用 ZenThemeKit 管理主题
- 使用 ZenUIKit 渲染组件

架构优势：
- 职责清晰：UI 组件与主题管理分离
- 完全独立：两个包互不依赖
- 灵活组合：可单独或配合使用
- 项目可正常编译运行
```

---

### Commit 6: docs: 添加集成文档和提交规范

**类型：** docs（文档）  
**范围：** 无（文档类型可省略范围）  
**标题：** docs: 添加集成文档和提交规范

**包含文件：**

```text
新增：
  .github/COMMIT_CONVENTION.md  # Git 提交规范
  docs/INTEGRATION_STATUS.md    # 集成状态报告
  docs/migration_guide.md       # 迁移指南
  docs/package_refactoring_todo.md  # TODO 跟踪
  提交计划.md                   # 提交计划
```

**提交信息：**

```text
docs: 添加集成文档和提交规范

- 添加 Git 提交规范文档
- 添加包集成状态报告
- 添加迁移指南文档
- 添加进度跟踪文档
- 添加提交计划文档

集成进度：
- 6/7 包已集成（85.7%）
- ArithmeticKit ✅
- FeedbackKit ✅
- HistoryKit ✅
- ZenUIKit ✅
- ZenThemeKit ✅
- ZenQuoteKit ✅
- ConversionKit ⏳（未集成，独立模块）
```

---

## 提交顺序

1. **chore(deps)** → 依赖配置和忽略规则
2. **refactor(feedback)** → FeedbackKit 集成
3. **refactor(history)** → HistoryKit 集成
4. **refactor(calculator)** → ArithmeticKit + ZenQuoteKit 集成
5. **refactor(ui)** → ZenUIKit + ZenThemeKit 集成
6. **docs** → 文档和规范

---

## 执行命令

### Commit 1: chore(deps)

```bash
git add .gitignore pubspec.yaml pubspec.lock pubspec_local.yaml
git commit -m "chore(deps): 配置包依赖和忽略规则

- 配置 7 个包的本地路径依赖
- 添加 pubspec_local.yaml 作为 GitHub 依赖备用
- 更新 .gitignore 忽略 packages/ 和 docs/archive/

包列表：
- ArithmeticKit (计算逻辑)
- ConversionKit (单位换算)
- ZenUIKit (UI 组件)
- ZenThemeKit (主题管理)
- ZenQuoteKit (禅语服务)
- FeedbackKit (反馈系统)
- HistoryKit (历史记录)"
```

### Commit 2: refactor(feedback)

```bash
git add lib/app/services/haptic_service.dart lib/app/services/audio_service.dart
git add lib/app/components/zen_calc_button.dart
git add lib/app/modules/calculator/views/widgets/calculator_top_bar.dart
git add lib/app/modules/history/views/history_view.dart
git commit -m "refactor(feedback): 集成 FeedbackKit

- 删除本地反馈服务实现
- 使用 FeedbackKit 的 HapticService 和 AudioService
- 创建 zen_calc_button.dart 包装组件
- 更新 4 个文件的导入和使用

功能保持：
- 触觉反馈（轻、中、重、选择）
- 音频反馈（数字、运算符、等于、清除）"
```

### Commit 3: refactor(history)

```bash
git add lib/app/services/history_service.dart
git add lib/app/services/calculation_history_service.dart
git add lib/app/modules/history/views/history_view.dart
git add lib/app/modules/history/controllers/history_controller.dart
git commit -m "refactor(history): 集成 HistoryKit

- 删除本地历史服务实现
- 创建 calculation_history_service.dart 适配器
- 使用 HistoryKit 的通用历史管理
- 更新 3 个文件使用新服务

功能增强：
- 通用历史记录模型
- 搜索和过滤功能
- 统计分析功能"
```

### Commit 4: refactor(calculator)

```bash
git add lib/app/utils/calculator_logic.dart
git add lib/app/utils/scientific_calculator_logic.dart
git add lib/app/services/zen_quote_service.dart
git add lib/app/components/zen_quote_widget.dart
git add lib/app/services/zen_settings_service.dart
git add lib/app/modules/calculator/views/calculator_view.dart
git commit -m "refactor(calculator): 集成 ArithmeticKit 和 ZenQuoteKit

- 删除本地计算逻辑实现
- 删除本地禅语服务实现
- 使用 ArithmeticKit 的 BasicCalculator
- 使用 ZenQuoteKit 的禅语服务和组件
- 添加 zen_settings_service.dart 管理设置

功能增强：
- 表达式计算支持
- 多语言禅语（中文、英文、日文）
- 上下文感知的禅语推荐"
```

### Commit 5: refactor(ui)

```bash
git add lib/app/components/neumorphic_button.dart
git add lib/app/components/neumorphic_container.dart
git add lib/app/components/neumorphic_display.dart
git add lib/app/components/zen_calc_display.dart
git add lib/app/components/zen_calc_button.dart
git add lib/app/config/theme/zen_calc_colors.dart
git add lib/app/config/theme/app_theme.dart
git add lib/main.dart
git add lib/app/modules/calculator/views/widgets/basic_button_grid.dart
git add lib/app/modules/calculator/views/widgets/scientific_button_grid.dart
git commit -m "refactor(ui): 集成 ZenUIKit 和 ZenThemeKit

ZenUIKit（纯 UI 组件库）：
- 重构为接受颜色参数的纯组件
- ZenButton 接受 backgroundColor、shadowLight、shadowDark
- ZenContainer 接受 backgroundColor、shadowLight、shadowDark
- ZenDisplay 接受可选颜色参数，提供默认值
- 删除对 ZenTheme 的依赖
- 删除 zen_theme_test.dart（主题测试移到 ZenThemeKit）

ZenThemeKit（主题管理）：
- 使用 ZenTheme 主题系统
- 使用 ZenThemeData 管理主题数据
- 提供 SandGarden 和 BambooForest 预设

主项目集成：
- 删除旧的 Neumorphic 组件
- zen_calc_button 从 ZenTheme 获取颜色并传递给 ZenButton
- zen_calc_display 使用 ZenTheme 获取颜色
- 使用 ZenThemeKit 管理主题
- 使用 ZenUIKit 渲染组件

架构优势：
- 职责清晰：UI 组件与主题管理分离
- 完全独立：两个包互不依赖
- 灵活组合：可单独或配合使用
- 项目可正常编译运行"
```

### Commit 6: docs

```bash
git add .github/COMMIT_CONVENTION.md
git add docs/INTEGRATION_STATUS.md
git add docs/migration_guide.md
git add docs/package_refactoring_todo.md
git add 提交计划.md
git commit -m "docs: 添加集成文档和提交规范

- 添加 Git 提交规范文档
- 添加包集成状态报告
- 添加迁移指南文档
- 添加进度跟踪文档
- 添加提交计划文档

集成进度：
- 6/7 包已集成（85.7%）
- ArithmeticKit ✅
- FeedbackKit ✅
- HistoryKit ✅
- ZenUIKit ✅
- ZenThemeKit ✅
- ZenQuoteKit ✅
- ConversionKit ⏳（未集成，独立模块）"
```

---

## 注意事项

1. **遵循提交规范**
   - 格式：`<类型>(<范围>): <简短描述>`
   - 标题不超过 50 字符（中文约 25 字符）
   - 使用现在时态："添加" 而非 "添加了"

2. **不提交 packages/ 目录**
   - packages/ 在 .gitignore 中
   - 这些包在独立仓库中维护
   - packages/ 中的修改（ZenUIKit 组件重构）已在独立仓库中完成

3. **逻辑分组**
   - 每个提交专注一个包的集成
   - 相关修改放在一起
   - 保持原子性

4. **保持可回滚**
   - 每个提交都是完整的功能
   - 可以独立回滚
   - 不破坏构建

5. **提交类型说明**
   - `chore`: 构建/依赖变更
   - `refactor`: 重构代码
   - `docs`: 文档变更

6. **重要修复说明**
   - ZenUIKit 组件已重构为纯 UI 组件（不依赖主题）
   - 主项目组件已更新以传递必需的颜色参数
   - 删除了 ZenUIKit 中的主题测试文件
   - 项目已验证可以正常编译和运行

---

**维护者：** ZenKitX Team  
**创建时间：** 2026-03-07  
**最后更新：** 2026-03-07  
**规范版本：** 遵循 `.github/COMMIT_CONVENTION.md`
